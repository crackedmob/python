# random number
# python does not have a random() function to make a random number ,
# but it has a built-in module random that can be used to make a random number:
import random

print(random.randrange(1, 10))
